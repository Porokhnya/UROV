Реализована проверка значений ТМС по трём каналам, сравнение их с эталонной величиной, и
при отклонении на значение (в процентах) большее, чем эталонная величина - принимается
решение об аварии.

Проверка РМС реализована в файле InterruptHandler.h, подсчёт РМС по запросу - в файле
ADCSampler.h